Amélioration de l'extraction

- pst: adresses smtp privilégiées, pas de doublons EX/SMTP
- pst: utilisation des ConversationIndex pour simuler les séquences In-Reply-To smtp
- pst: ajout d'un return-path fake si dans un serveur local exchange
- gère les différents type d'attachement
- tous: utilisation d'une seule adresse From, alerte si plusieurs
- tous: récupération de tous les formats de Body
- tous: génération d'un eml fake à partir de StoreMessage avec toutes les métadonnées et attachements
Attention1: à ce stade tous les inline sont attachés au corps HTML, ou à la racine si pas de corps HTML
Attention2: dans le cas pst, tous les attachements qui ne sont pas de type Store sont mis en inline, car la notion d'inline ne semble pas exister dans les pst
